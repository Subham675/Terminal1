{
    "name": "terminal1/canteen-booking",
    "description": "Terminal 1 — The Restaurant: PHP/MySQL booking system with Razorpay payments",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": ">=8.1",
        "ext-pdo": "*",
        "ext-curl": "*",
        "ext-fileinfo": "*",
        "phpmailer/phpmailer": "^7.1"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5"
    },
    "suggest": {
        "phpmailer/phpmailer": "Enables SMTP email sending with PDF attachments (invoices). Without it, OTP emails still send via PHP's mail(), but attachments are skipped.",
        "dompdf/dompdf": "Enables PDF invoice generation for booking confirmations."
    },
    "autoload": {
        "classmap": [
            "app/config/",
            "app/controllers/",
            "app/models/",
            "app/middleware/"
        ]
    },
    "scripts": {
        "test": "phpunit --testdox tests"
    }
}
